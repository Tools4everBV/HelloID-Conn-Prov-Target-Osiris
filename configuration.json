[
    {
        "key": "BaseUrl",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "BaseUrl",
            "description": "The URL to the API.",
            "required": true
        }
    },
    {
        "key": "ApiKey",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "type": "password",
            "label": "ApiKey",
            "description": "The API key to connect to the API",
            "required": true
        }
    },
    {
        "key": "UpdatePersonOnCorrelate",
        "type": "checkbox",
        "defaultValue": false,
        "templateOptions": {
            "label": "Update account during correlating",
            "required": false,
            "description": "This will update the account in the target application during correlation"
        }
    },
    {
        "key": "IsDebug",
        "type": "checkbox",
        "defaultValue": true,
        "templateOptions": {
            "label": "Toggle debug logging",
            "description": "When toggled, debug logging will be displayed",
            "required": false
        }
    }
]